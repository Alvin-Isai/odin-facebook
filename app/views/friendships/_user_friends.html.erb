<div class='column'>  
  
  <nav class='panel'>
    <p class='panel-heading'>Friends</p>
  </nav>
  
  <% if @friends.any? %>
    <% current_user.friends.each do |friend| %>
      <div class='panel-block'>
        <article class='media'>
          <div class='media-left'>
            <figure class='image is-32x32'>
              <%= image_tag friend.profile.avatar.url(:thumb) %>
            </figure>
          </div>
          <div class='media-content'>
            <div class='content'>
              <p>
                <%= friend.profile.fname + ' ' + friend.profile.lname%>
              </p>
            </div>
          </div>
        </article>
      </div>
    <% end %>
  <% else %>
    <p>You have no friends</p>
  <% end %>
</div>