<%= form_for([@post, @post.comments.build]) do |f| %>
  <%= f.text_field :comment %><br>
  <%= f.hidden_field :user_id, :value => current_user.id %><br>
  <%= f.submit 'Leave a reply' %><br>
<% end %>